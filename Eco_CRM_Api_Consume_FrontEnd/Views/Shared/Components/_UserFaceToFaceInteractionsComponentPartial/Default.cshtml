@model List<DisplayUserFaceToFaceInteractionCountResponse>

<div class="info-box">
    <h4>En Çok Yüz Yüze Görüşülen</h4>
    <ul id="mostFaceToFaceList">
        @foreach (var x in Model)
        {
            <li>
                <a href="/CustomerOperation/OperationsByCustomer/@x.CustomerId">@x.CompanyName</a>
                <span>@x.InteractionCount</span>
            </li>
        }
    </ul>
</div>